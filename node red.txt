#resistor to 10 and led to 6, both to each other

# install nodejs and nodered 
# start nodered by typing 'node-red' in terminal
# open nodered interface in browser at localhost:1880
# install dashboard by going to menu->manage pallette-> install tab -> dashboard env-> install
# create web ui-> under dashboard on right side, create group and tab -> take switch from palette and configure -> gpio from palette and configure 
# deploy and open 127.0.0.1:1880/ui
# connect led and try controlling through ui
# ip address of rpi (ifconfig in terminal), control through phone 

# sudo date -s 'YYYY-MM-DD HH:MM:SS'
#shorter wire to ground, longer to rpi
# pinout command to see rpi pins